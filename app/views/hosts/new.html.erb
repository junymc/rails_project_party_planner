<h1>New Host</h1>
<p>Please fill out the form below to complete the sign up process.</p>

<%= form_with(model: @host, url: [@party, @host], local: true) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<br>

<%= f.submit "Create an account as a host" %>
<% end %>

<% if @host.errors.any? %>
  <% @host.errors.full_messages.each do |m| %>
    <p class="danger"><%= m %></p>
  <% end %>
<% end %>